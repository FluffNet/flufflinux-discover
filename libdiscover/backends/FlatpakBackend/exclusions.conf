# Flatpak applications hidden by Fluff Linux Discover.
#
# Add one Flatpak application ID per line. Matching is case-insensitive.
# End an entry with * to exclude that ID and all IDs beginning with it.

org.winehq.Wine*
